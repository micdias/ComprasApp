<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.br.compras">


    <application
        android:allowBackup="true"
        android:icon="@mipmap/empty_cart"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@android:style/Theme.Holo.Light">
        <activity android:name=".activity.ListaSetorActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.CadastroSetorActivity" />
        <activity android:name=".activity.ListaComprasActivity"/>
        <activity android:name=".activity.CadastroItemCompraActivity"/>

    </application>

</manifest>
